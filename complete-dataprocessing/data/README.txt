THIS FOLDER ONLY CONTAINS DATA
The data is from 3 subjects. Each subject has data in the form of;
	One (? x 2) matrix with number of steps per minute.
	One (? x 2) matrix with timestamp and heartrate data
	One (? x 2) matrix with bedtime-timestamp and awoke-timestamp
	One (? x 2) matrix with timestamp and amount of fluid ingested
	One (? x 2) matrix with headache start time and headache ending time
	
This gives a (? x 10) matrix with data per subject without any headers og table names
