THIS FOLDER ONLY CONTAINS DATA
The data is from 3 subjects. Each subject has data i the form of;
	One (? x 1) vector with number of steps per minute.
	One (? x 2) matrix with timestamp and heartrate data
	One (? x 2) matrix with bedtime-timestamp and awoke-timestamp
	One (? x 2) matrix with timestamp and amount of fluid ingested
	
This gives a (? x 7) matrix with data per subject
